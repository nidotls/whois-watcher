WORK_DIR=./.state/
WATCH_DOMAINS=example.com example.org
NOTIFY_DISCORD_WEBHOOK=https://discord.com/api/webhooks/...
NOTIFY_DISCORD_USER_ID=

# Optional: Delay between checking each domain (default: 2s)
DOMAIN_CHECK_DELAY=2s

# Optional: How long to cache the IANA RDAP bootstrap file (default: 12h)
BOOTSTRAP_CACHE_TTL=12h

# Optional: Cron schedule for built-in scheduler (standard cron format)
# If not set, runs once and exits (for use with external cron/scheduler)
# Examples:
#   "0 */12 * * *"  - Every 12 hours
#   "0 8,20 * * *"  - At 8:00 and 20:00 daily
#   "0 0 * * *"     - Daily at midnight
#   "@every 12h"    - Every 12 hours (robfig/cron syntax)
#   "@daily"        - Once a day at midnight
CRON_SCHEDULE=